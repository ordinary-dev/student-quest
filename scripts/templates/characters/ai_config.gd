extends Node

# The settings for the NPCs.
# You must attach it to an object
# that has the same parent as the NPC.

export (NodePath) var nav_node
export (NodePath) var upper_left_node
export (NodePath) var bottom_right_node
